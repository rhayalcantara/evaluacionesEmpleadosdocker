# T2 — Ronda 2: corregir un error de tipos en `src/app/Controllers/HistorialEvaluacion.ts`

Tu archivo **`src/app/Controllers/HistorialEvaluacion.ts` YA EXISTE en tu directorio** y está
completo. **NO lo reescribas**: usa `leer_archivo` y aplica UNA corrección puntual con
`editar_archivo`.

**NO uses `ejecutar_bash`** (el shell se cuelga en este entorno). Solo `leer_archivo` y
`editar_archivo`. El orquestador compila y prueba al recibir el archivo.

## CORRECCIÓN OBLIGATORIA (única)

1. **Error de compilación** `TS2740` en `getHistorialConFiltros` (~línea 161-165):
   ```ts
   respuesta: this.datos.getdatos<ModelResponse>(this.datos.URL + '/api/Evaluacions'),
   ...
   const evaluaciones: IEvaluacion[] = respuesta?.data ?? [];
   ```
   La firma real es `getdatos<T = any>(url): Observable<ModelResponse<T>>`, es decir, YA envuelve
   en `ModelResponse`; con `<ModelResponse>` el `.data` queda tipado como `ModelResponse` y no
   como `IEvaluacion[]` (el error lo indujo la spec original, no es culpa tuya). Cambia el
   parámetro de tipo a `IEvaluacion[]`:
   ```ts
   respuesta: this.datos.getdatos<IEvaluacion[]>(this.datos.URL + '/api/Evaluacions'),
   ```
   y deja `const evaluaciones: IEvaluacion[] = respuesta?.data ?? [];` tal cual.

No cambies nada más. Responde LISTO al terminar.
