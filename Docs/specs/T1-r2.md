# T1 — Ronda 2: corregir `src/app/Helpers/historial-utils.ts` según la batería

Tu archivo **`src/app/Helpers/historial-utils.ts` YA EXISTE en tu directorio** y está casi
correcto (22 de 23 pruebas en verde, compila). **NO lo reescribas desde cero**: usa
`leer_archivo` y aplica UNA corrección puntual con `editar_archivo`.

## CORRECCIÓN OBLIGATORIA (única)

1. **Campos de texto del empleado vacíos deben ser `''`, nunca `undefined`.**
   En `mapearResumen` (~líneas 171-175) escribes:
   ```ts
   empleadoIdentificacion: ... || undefined,
   codigoUsuario: aTexto(fuenteEmpleado?.codigousuario) || undefined,
   departamento: aTexto(fuenteEmpleado?.departamento) || undefined,
   puesto: aTexto(fuenteEmpleado?.cargo) || undefined,
   ```
   El contrato dice: "empleadoIdentificacion, codigoUsuario, departamento, puesto(=cargo): el valor
   (trim) o `''`". Cambia los cuatro a devolver la cadena vacía cuando no haya valor, por ejemplo:
   ```ts
   empleadoIdentificacion: aTexto(fuenteEmpleado?.identificacion),
   codigoUsuario: aTexto(fuenteEmpleado?.codigousuario),
   departamento: aTexto(fuenteEmpleado?.departamento),
   puesto: aTexto(fuenteEmpleado?.cargo),
   ```
   (`aTexto` ya devuelve `''` para null/undefined y aplica trim; si en tu versión
   `empleadoIdentificacion` usa otra expresión, ajústala con el mismo criterio.)

   Prueba que falla hoy: `mapearResumen(ev)` sin empleado → se espera
   `empleadoIdentificacion === ''`, `codigoUsuario === ''`, `departamento === ''`, `puesto === ''`.

## Verificación antes de responder LISTO
- `npx tsc --noEmit -p tsconfig.json` sin errores.
- No cambies ninguna otra función ni el orden de `ESTADOS_EVALUACION`.
- Sigue sin importar `@angular/*` ni `rxjs`.

## Entregable
El mismo archivo `src/app/Helpers/historial-utils.ts`, editado en su sitio. Nada más.
