# B5 — Ronda 3: `error` debe iniciar en cadena vacía, no en `null`

Los dos archivos de `src/app/Views/Components/evaluacioncomponents/bitacora-resumen/` **YA EXISTEN**, compilan
y la plantilla es correcta. La batería falla en un solo caso: la spec B5.md define `error: string` (vacío =
sin error) y el componente lo inicializa en `null`.

## Corrección exacta (solo en `bitacora-resumen.component.ts`)
1. Declara `error: string = '';` (no `string | null`, no `null`).
2. Donde se limpie el error antes de cargar, asigna `''`; donde se capture el fallo HTTP asigna
   `this.error = this.bitacora.mensajeError(err)` (string). Cuando los inputs son 0 y no se llama al API,
   `error` debe quedar `''` y `fila` en `null`.
3. La plantilla usa `*ngIf="error"`: con `''` sigue funcionando; no la toques.

## Verificación antes de responder LISTO
Relee el `.ts`: ninguna asignación `error = null`; el tipo es `string`. Nada más cambia.

## RESTRICCIÓN OPERATIVA
**NO uses `ejecutar_bash`.** Solo `leer_archivo` y `editar_archivo`. Edita y responde LISTO.
