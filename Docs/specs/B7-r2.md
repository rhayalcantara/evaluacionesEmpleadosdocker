# B7 — Ronda 2: dos errores de compilación en `reporte-bitacora.component.ts`

Los tres archivos de `src/app/Views/Components/Pages/reporte-bitacora/` **YA EXISTEN**. `tsc` (ejecutado por el
orquestador) falla solo aquí:

```
reporte-bitacora.component.ts(102,13): error TS2322: Type 'Date' is not assignable to type 'string'.
reporte-bitacora.component.ts(103,13): error TS2322: Type 'Date' is not assignable to type 'string'.
```

Causa: `IPeriodo.fechaInicio`/`fechaFin` están tipadas como `Date` en `src/app/Models/Periodos/IPeriodo.ts`,
y el objeto que construyes en `aplicar({...})` (aprox. líneas 99-105) espera `string` (tipo `IPeriodoActivo`).

## Corrección exacta
En ese bloque, convierte las fechas a texto con `fechaISO` de `src/app/Helpers/bitacora-utils` (acepta `Date`
o `string` y devuelve `'YYYY-MM-DD'`):
```ts
fechaInicio: fechaISO(p.fechaInicio),
fechaFin: fechaISO(p.fechaFin),
```
Importa `fechaISO` junto a las demás utilidades. Alternativa equivalente: `String(p.fechaInicio ?? '')`,
pero `fechaISO` es la correcta porque `rangoPeriodo` la usa después.

**NO reescribas el archivo: edítalo** con `editar_archivo` solo en ese punto (y el import). No toques
`.html`, `.css` ni ningún otro archivo.

## Verificación antes de responder LISTO
Relee las líneas 95-110: no queda ninguna asignación de `Date` a un campo `string`; `fechaISO` está importada.

## RESTRICCIÓN OPERATIVA
**NO uses `ejecutar_bash`.** Solo `leer_archivo` y `editar_archivo`. Edita y responde LISTO.
