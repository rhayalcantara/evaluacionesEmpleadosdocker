# T3 — Componente `historial-evaluaciones` (estados reales, búsqueda y medio año en pantalla)

Tu directorio de trabajo contiene una copia completa de `src/` del proyecto Angular 16. Los dos
archivos a modificar YA EXISTEN:

- `src/app/Views/Components/Pages/historial-evaluaciones/historial-evaluaciones.component.ts`
- `src/app/Views/Components/Pages/historial-evaluaciones/historial-evaluaciones.component.html`

Léelos primero y aplica los cambios con `editar_archivo` (ediciones puntuales; NO reescribas
desde cero lo que no cambia). **No modifiques ningún otro archivo** (ni el `.css`, ni los modales,
ni el controlador, ni `historial-utils.ts`).

Compila con `npx tsc --noEmit -p tsconfig.json` desde tu directorio (~1 min). Los templates HTML
no los valida tsc: revisa a mano que cada propiedad/método usado en el HTML exista en la clase.

## Contexto
Lee `src/app/Helpers/historial-utils.ts` (exporta `ESTADOS_EVALUACION`, `etiquetaEstado`,
`claseEstado`, `coincideEstado`, `coincideBusqueda`, `puedeComparar`) y
`src/app/Models/HistorialEvaluacion/IHistorialEvaluacion.ts` (el resumen trae ahora
`esMedioAno`, `periodoTipo`, `codigoUsuario`; las estadísticas traen `evaluacionesFinales`,
`evaluacionesMedioAno` y `mejorEvaluacion` puede ser `null`). El controlador
`HistorialEvaluacion` (`src/app/Controllers/HistorialEvaluacion.ts`) conserva los mismos
métodos públicos que hoy usa el componente; ya devuelve nombres e identificaciones resueltos y
rechaza con error (con `message`) las comparaciones no válidas.

Problemas que corriges: (a) el filtro de estado listaba estados inventados
(`Completada, Pendiente, En Proceso, Aprobada, Rechazada`) que no existen en el sistema;
(b) la búsqueda no encontraba por secuencial ni usuario; (c) las evaluaciones de medio año se
mostraban y comparaban como si tuvieran nota.

## Cambios en `historial-evaluaciones.component.ts`

1. Importa desde `'../../../../Helpers/historial-utils'`: `ESTADOS_EVALUACION`, `etiquetaEstado`,
   `claseEstado`, `coincideEstado`, `coincideBusqueda`, `puedeComparar`. Importa
   `IEstadoEvaluacionOpcion` desde el modelo.
2. `estadosDisponibles: IEstadoEvaluacionOpcion[] = ESTADOS_EVALUACION;` (elimina la lista fija).
3. Nuevo método público `etiquetaEstado(estado: string | null | undefined): string` que delega en
   la función homónima de utils.
4. `getEstadoClass(estado)` pasa a devolver `claseEstado(estado)` (borra la lógica actual de
   `includes('completada')` etc.).
5. En `aplicarFiltrosLocales()`:
   - búsqueda de texto: `resultado.filter(item => coincideBusqueda(item, this.textoBusqueda))`
     (sustituye el filtro manual por nombre/identificación/periodo/departamento).
   - filtro de estado: aplica solo si `this.filtros.estadoEvaluacion` es una cadena no vacía, con
     `coincideEstado(item.estadoEvaluacion, this.filtros.estadoEvaluacion)`.
   - filtro de periodo: aplica solo si `this.filtros.periodoId` es un número (`typeof === 'number'`).
6. `toggleSeleccion(evaluacionId)`: antes de agregar, busca el item en `this.historial`; si
   `item?.esMedioAno` es true → `showMessage('Las evaluaciones de medio año no se comparan', 'Información', 'info')`
   y retorna sin agregar. (Quitar de la selección siempre se permite.)
7. `compararEvaluaciones()`: tras validar que hay exactamente 2 ids, busca ambos items en
   `this.historial`; si ambos existen, evalúa `puedeComparar(a, b)` y si `ok` es false →
   `showMessage(motivo, 'Información', 'info')` y retorna SIN llamar al controlador. En el
   `error` del observable muestra `error?.message || 'Error al comparar evaluaciones'` como
   `'error'` (hoy muestra un texto fijo).
8. Nuevo getter público `hayFinalesEnEstadisticas: boolean` → `!!this.estadisticas && this.estadisticas.evaluacionesFinales > 0`.
9. Nuevo método público `esMedioAno(item: IHistorialEvaluacionResumen): boolean` → `!!item?.esMedioAno`.
10. El rol se compara siempre en minúsculas (`'admin' | 'supervisor' | 'empleado'`, como ya
    asigna `inicializarComponente`). Corrige el HTML en consecuencia (ver abajo).
11. Elimina imports que queden sin uso. No agregues `console.log`.

## Cambios en `historial-evaluaciones.component.html`

1. Filtro de estado: `<mat-option *ngFor="let estado of estadosDisponibles" [value]="estado.valor">{{ estado.etiqueta }}</mat-option>`.
2. Placeholder de búsqueda: `"Nombre, cédula, secuencial, usuario, departamento..."`.
3. Fila de la tabla (`<tr *ngFor="let item of displayedHistorial" ...>`): agrega
   `[class.fila-medio-ano]="esMedioAno(item)"` y el atributo `[attr.data-tipo]="item.periodoTipo"`.
4. Checkbox de comparar: `[disabled]="esMedioAno(item)"` y
   `[title]="esMedioAno(item) ? 'Las evaluaciones de medio año no se comparan' : 'Seleccionar para comparar'"`.
5. Celda Período: después de `{{ item.periodoNombre }}` agrega
   `<span class="badge badge-medio-ano" *ngIf="esMedioAno(item)">Medio año</span>`.
6. Celda Estado: el badge muestra `{{ etiquetaEstado(item.estadoEvaluacion) }}` (no el valor crudo).
7. Celdas Total / Desempeño / Competencias: para medio año NO muestres números. Usa en cada celda
   un `<ng-container *ngIf="!esMedioAno(item); else sinPuntuacion">` con el contenido actual, y
   define UNA sola vez al final del archivo
   `<ng-template #sinPuntuacion><span class="sin-puntuacion" title="Evaluación de medio año: sin puntuación comparable">—</span></ng-template>`.
8. Panel de estadísticas:
   - En el primer `stat-item` (Total Evaluaciones) agrega debajo del valor
     `<small>Finales: {{ estadisticas.evaluacionesFinales }} · Medio año: {{ estadisticas.evaluacionesMedioAno }}</small>`.
   - Envuelve los `stat-item` de Promedio General, Mejor Evaluación, Tendencia, Promedio
     Desempeño y Promedio Competencias en `<ng-container *ngIf="hayFinalesEnEstadisticas; else sinFinales">`
     y define `<ng-template #sinFinales><div class="stat-item stat-item-vacio"><mat-icon>info</mat-icon><div><p class="stat-label">Sin evaluaciones finales puntuables</p><p class="stat-value">Las evaluaciones de medio año no promedian</p></div></div></ng-template>`.
   - En Mejor Evaluación usa `estadisticas.mejorEvaluacion?.totalCalculo` y
     `estadisticas.mejorEvaluacion?.periodoNombre` (puede ser null).
9. Rol en minúsculas: el `<p>` del estado vacío usa `*ngIf="rolUsuario === 'admin'"`; el botón de
   estadísticas usa `*ngIf="rolUsuario !== 'empleado'"`.

## Criterios de aceptación
1. `estadosDisponibles` es exactamente `ESTADOS_EVALUACION` (6 opciones; el select muestra las
   etiquetas y filtra por el valor crudo).
2. Escribir `525` o `ralcantara` en Buscar encuentra las filas de ese empleado.
3. Una fila de medio año lleva la clase `fila-medio-ano`, muestra el badge "Medio año", las tres
   celdas numéricas muestran "—" y su checkbox está deshabilitado.
4. `toggleSeleccion` de un id de medio año no lo agrega y avisa; `compararEvaluaciones` con una
   de medio año avisa con el motivo de `puedeComparar` y NO llama al controlador.
5. El badge de estado muestra la etiqueta ("Evaluado por supervisor", "Sin iniciar"…), no el
   valor crudo.
6. El panel de estadísticas muestra el conteo Finales/Medio año y, sin finales, el mensaje de
   "Sin evaluaciones finales puntuables" en lugar de promedios; no explota con
   `mejorEvaluacion` null.
7. Compila; solo cambian los dos archivos indicados.

## Entregable
Los dos archivos editados en su sitio. Nada más.
