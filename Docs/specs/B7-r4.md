# B7 — Ronda 4: dos correcciones puntuales

Los tres archivos de `src/app/Views/Components/Pages/reporte-bitacora/` **YA EXISTEN**, compilan y pasan la batería.
El crítico los RECHAZÓ por estos dos puntos. **NO reescribas: edita** con `editar_archivo`. Nada más cambia.

## Crítica 1 — `nombresCompetencias` duplicado (solo `.ts`)
`reporte-bitacora.component.ts` (aprox. líneas 252-256) reimplementa `nombresCompetencias`, que ya existe en
`src/app/Helpers/bitacora-utils.ts`. **Corrección:** añade `nombresCompetencias as nombresCompetenciasUtil` al
import de `../../../../Helpers/bitacora-utils` (la ruta relativa que ya usa el archivo) y deja el método como
delegación: `nombresCompetencias(ev: IBitacoraEvento): string { return nombresCompetenciasUtil(ev); }`.
La plantilla sigue llamando `nombresCompetencias(ev)`; no la toques por esto.

## Crítica 2 — el select de supervisor convierte el secuencial en texto (solo `.html`)
En `reporte-bitacora.component.html` (aprox. línea 9) la `<option>` del `select` de supervisor usa
`[value]="s.secuencial"`, así que al elegir en la interfaz `supervisorSel` pasa a ser `'90'` (string) y
`equipo()` recibe un string. **Corrección:** cambia a `[ngValue]="s.secuencial"` (el `select` ya tiene
`[(ngModel)]="supervisorSel"`). Nada más del template cambia.

## Verificación antes de responder LISTO
Relee: el `.ts` no define lógica propia de nombres de competencias; el `.html` no tiene `[value]="s.secuencial"`.

## RESTRICCIÓN OPERATIVA
**NO uses `ejecutar_bash`.** Solo `leer_archivo` y `editar_archivo`. Edita los dos archivos y responde LISTO.
