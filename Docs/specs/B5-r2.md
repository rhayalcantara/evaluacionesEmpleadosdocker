# B5 — Ronda 2: falta la plantilla `bitacora-resumen.component.html`

En tu directorio de trabajo **YA EXISTE y compila**
`src/app/Views/Components/evaluacioncomponents/bitacora-resumen/bitacora-resumen.component.ts`
(léelo primero: ya tiene la caché estática, `ngOnChanges`, `fila`, `cargando`, `error`, `abierto`, `texto`,
`hayEventos`, `alternar()`, `fechaCorta`, `claseTipo`, `claseImpacto`, `modo` y `styles` inline).
El lanzamiento anterior se detuvo sin escribir la plantilla. **Tu única tarea ahora es crear**
`src/app/Views/Components/evaluacioncomponents/bitacora-resumen/bitacora-resumen.component.html`.
No modifiques el `.ts` salvo que descubras que le falta algo que la plantilla necesite (en ese caso, edítalo
mínimamente). No crees ningún otro archivo. La spec original (B5.md) sigue vigente.

## Plantilla exacta que debes escribir
- Contenedor raíz: `<div class="bitacora-resumen" [class.sin-eventos]="!hayEventos">`.
- Si `cargando`: `<small class="text-muted">Cargando bitácora…</small>`.
- Si no y hay `error`: `<small class="text-danger" [title]="error">Bitácora no disponible</small>`.
- Si no (estado normal):
  - `<small class="resumen-texto">{{ texto }}</small>`
  - si `hayEventos && modo === 'compacto'`:
    `<button type="button" class="btn btn-link btn-sm p-0 ms-1" (click)="alternar()">{{ abierto ? 'Ocultar' : 'Ver' }}</button>`
  - si `hayEventos && (abierto || modo === 'lista')`:
    ```html
    <ul class="lista-eventos">
      <li *ngFor="let ev of fila?.eventos">
        <span class="badge" [ngClass]="claseTipo(ev.tipo)">{{ ev.tipo }}</span>
        <span class="badge" [ngClass]="claseImpacto(ev.impacto)">{{ ev.impacto }}</span>
        <span class="fecha">{{ fechaCorta(ev.fechaEvento) }}</span>
        <div class="descripcion">{{ ev.descripcion }}</div>
        <small class="text-muted">{{ ev.registradoPorNombre }}</small>
      </li>
    </ul>
    ```
- Usa `*ngIf` con `ng-container`/`ng-template` (el componente importa `CommonModule`). Sin lógica compleja en
  la plantilla: solo los getters y métodos que ya existen en el `.ts`.

## Verificación antes de responder LISTO
Relee el `.ts` y confirma que cada identificador que usa la plantilla existe con ese nombre exacto
(`cargando`, `error`, `texto`, `hayEventos`, `modo`, `abierto`, `alternar`, `fila`, `claseTipo`,
`claseImpacto`, `fechaCorta`). Si falta alguno, añádelo al `.ts` con la firma de B5.md.

## RESTRICCIÓN OPERATIVA
**NO uses `ejecutar_bash`.** Solo `leer_archivo`, `listar_directorio` y `editar_archivo`. Lee el `.ts`,
escribe el `.html` completo de una sola vez y responde LISTO.
